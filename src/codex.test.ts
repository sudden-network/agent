describe('codex', () => {
  // TODO: regression test for MCP startup when resume is enabled:
  // restoring ~/.codex must not clobber the current run's MCP server URL (stale localhost port -> connection refused).
  test.todo('starts MCP successfully when resuming a restored session');
});

